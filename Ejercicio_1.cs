/*Ejercicio 1: Impresión de Números Pares

using System;

class Program
{
    static void Main()
    {
        for (int i = 2; i <= 20; i++)
        {
            if (i % 2 == 0)
            {
                Console.WriteLine(i);
            }
        }
    }
}

Escribe un programa
que muestre los números pares del 2 al 20. */
